#pragma once
#include <string>
#include <map>
namespace lte {
	class InIReader
	{
		public:
			void LoadData(std::string path);
			void SaveData(std::string path);
			std::map<std::string,std::map<std::string,


	};
}